package objectorientedprogramming

/**
 *  Object Expressions
 *
 *      Replace Java Anonymous Classes
 *
 *      Bear in mind that if the interface declares a single method then
 *          we use a lambda expressions
 *          therefore, object expressions applies when more than one method is declared
 *
 *
 *      window.addMouseListener(
 *
 *          object : MouseAdapter() {
 *
 *              override fun mouseClicked(e: MouseEvent) {
 *              }
 *
 *              override fun mouseEntered(e: MouseEvent) {
 *              }
 *
 *          }
 *
 *      )
 */


